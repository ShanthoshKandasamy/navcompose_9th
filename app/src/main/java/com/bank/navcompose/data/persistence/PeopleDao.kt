package com.bank.navcompose.data.persistence

/*import androidx.room.Dao
import androidx.room.Insert
import androidx.room.OnConflictStrategy
import androidx.room.Query
import com.bank.navcompose.data.model.People

@Dao
interface PeopleDao {

  @Insert(onConflict = OnConflictStrategy.REPLACE)
  suspend fun insertPeopleList(posters: List<People>)

  @Query("SELECT * FROM People WHERE id = :id_")
  suspend fun getPeople(id_: Long): People?

  @Query("SELECT * FROM People")
  suspend fun getPeopleList(): List<People>
}*/
